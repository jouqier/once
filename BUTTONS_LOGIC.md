# Логика работы кнопок (новая версия)

## Обзор

Полностью переписанная логика кнопок управления контентом. Код упрощен, убраны избыточные проверки и сложные механизмы кеширования.

## Состояния

### Фильмы
- `none` - не добавлен
- `want` - хочу посмотреть
- `watched` - просмотрено

### Сериалы
- `none` - не добавлен
- `want` - хочу посмотреть
- `watching` - смотрю (есть просмотренные эпизоды)
- `watched` - просмотрено (все эпизоды)

---

## 🎬 Фильмы

### Кнопка "Want"

**Состояние: none**
- Клик → добавить в Want
- Результат: состояние `want`, кнопка активна, кнопка Watched скрыта

**Состояние: want (активна)**
- Клик → открыть меню:
  - **Move to Watched** → удалить из Want, открыть диалог оценки
  - **Remove from Want** → удалить из Want, состояние `none`

**Состояние: watched**
- Скрыта

---

### Кнопка "Watched"

**Состояние: none**
- Клик → открыть диалог оценки
- После оценки: добавить в Watched, состояние `watched`, кнопка Want скрыта

**Состояние: watched (активна)**
- Клик → открыть меню:
  - **Move to Want** → удалить из Watched, удалить оценку, добавить в Want
  - **Remove from Watched** → удалить из Watched, удалить оценку, состояние `none`
  - **Edit Review** → открыть диалог редактирования оценки

**Состояние: want**
- Скрыта

---

## 📺 Сериалы

### Кнопка "Want"

**Состояние: none**
- Клик → добавить в Want
- Результат: состояние `want`, кнопка активна, кнопка Watched скрыта

**Состояние: want (активна)**
- Клик → открыть меню:
  - **Remove from Want** → удалить из Want, состояние `none`
  - **I'm watching this** → удалить из Want, добавить в Watching, состояние `watching`
  - **Mark all as Watched** → удалить из Want, добавить в Watched, состояние `watched`

**Состояния: watching, watched**
- Скрыта

---

### Кнопка "Watching"

**Состояние: watching (активна)**
- Клик → открыть меню:
  - **Remove from Watching** → удалить из Watching, состояние `none`
  - **Mark all as Watched** → удалить из Watching, добавить в Watched, состояние `watched`
  - **Move to Want** → удалить из Watching, добавить в Want, состояние `want`

**Другие состояния**
- Скрыта

---

### Кнопка "Watched"

**Состояние: none**
- Клик → открыть меню:
  - **Mark all as Watched** → добавить в Watched, состояние `watched`
  - **I'm watching this** → добавить в Watching, состояние `watching`

**Состояние: watched (активна)**
- Клик → открыть меню:
  - **Remove from Watched** → удалить из Watched, удалить оценки сезонов, состояние `none`
  - **Move to Want** → удалить из Watched, удалить оценки сезонов, добавить в Want, состояние `want`

**Состояние: want**
- Скрыта

---

## 🔄 Автоматические переходы (сериалы)

### Переход в WATCHING
**Триггер:** отметка первого эпизода  
**Условие:** состояние `none` или `want`  
**Действие:** удалить из Want (если был), добавить в Watching

### Переход в WATCHED
**Триггер:** отметка последнего непросмотренного эпизода  
**Условие:** состояние `watching`  
**Действие:** удалить из Watching, добавить в Watched

### Переход обратно в WATCHING
**Триггер:** снятие отметки с эпизода  
**Условие:** состояние `watched`, до этого все эпизоды были просмотрены  
**Действие:** удалить из Watched, добавить в Watching

---

## 🎨 Визуальные состояния

### Фильмы

| Состояние | Want | Watched |
|-----------|------|---------|
| none | Серая | Серая |
| want | ✓ Want (прозрачная с границей) | Скрыта |
| watched | Скрыта | ✓ Watched (primary-container) |

### Сериалы

| Состояние | Want | Watching | Watched |
|-----------|------|----------|---------|
| none | Серая | Скрыта | Серая |
| want | ✓ Want (прозрачная с границей) | Скрыта | Скрыта |
| watching | Скрыта | Watching (primary-container) | Скрыта |
| watched | Скрыта | Скрыта | ✓ Watched (primary-container) |

---

## 📋 События

### Фильмы
- `review-submitted` - оценка отправлена
- `review-removed` - оценка удалена

### Сериалы
- `episode-checkbox-changed` - изменение чекбокса эпизода (для автоматических переходов)
- `season-reviews-removed` - оценки сезонов удалены

---

## 🎭 Кнопки под списком эпизодов сезона

### Логика работы

Под каждым списком эпизодов отображаются кнопки управления сезоном:

**Если сезон НЕ полностью просмотрен:**
```
┌─────────────────────────┐
│  Mark as Watched        │
└─────────────────────────┘
```

**Если сезон полностью просмотрен БЕЗ оценки:**
```
┌─────────────────────────┬─────────────────────────┐
│  Rate Season            │  Mark as Unwatched      │
└─────────────────────────┴─────────────────────────┘
```

**Если сезон полностью просмотрен С оценкой:**
```
┌─────────────────────────┐
│  Mark as Unwatched      │
└─────────────────────────┘
```

---

### Кнопка "Mark as Watched"

**Действие:**
- Отмечает все эпизоды текущего сезона как просмотренные
- Обновляет все чекбоксы эпизодов
- Убирает точки "непросмотрено"
- Проверяет, все ли сезоны теперь просмотрены
- Если да → отправляет событие `all-seasons-watched` (автоматический переход в WATCHED)
- Обновляет кнопки сезона (показывает "Rate Season" и "Mark as Unwatched")

---

### Кнопка "Mark as Unwatched"

**Действие:**
- Снимает отметки со всех эпизодов текущего сезона
- Обновляет все чекбоксы эпизодов
- Добавляет точки "непросмотрено"
- Проверяет, были ли до этого все сезоны просмотрены
- Если да → отправляет событие `all-seasons-unwatched` (автоматический переход в WATCHING)
- Обновляет кнопки сезона (показывает "Mark as Watched")

---

### Кнопка "Rate Season"

**Действие:**
- Открывает диалог оценки для конкретного сезона
- Позволяет поставить рейтинг (1-10) и написать отзыв
- Сохраняет оценку отдельно для каждого сезона
- После сохранения кнопка скрывается (остается только "Mark as Unwatched")

---

## 🔄 Взаимодействие с основными кнопками

### События от основных кнопок → кнопки сезонов

Основные кнопки сериала отправляют событие `tv-action` с действиями:

**`mark-all-watched`**
- Отмечает все эпизоды всех сезонов как просмотренные
- Обновляет UI текущего сезона

**`mark-all-unwatched`**
- Снимает отметки со всех эпизодов всех сезонов
- Обновляет UI текущего сезона

**`clear-all-seasons`**
- Снимает отметки со всех эпизодов всех сезонов
- Обновляет UI текущего сезона

### События от кнопок сезонов → основные кнопки

**`all-seasons-watched`**
- Отправляется когда последний сезон становится полностью просмотренным
- Основные кнопки переводят сериал в состояние WATCHED

**`all-seasons-unwatched`**
- Отправляется когда снимаются отметки с сезона, и теперь не все сезоны просмотрены
- Основные кнопки переводят сериал из WATCHED в WATCHING

**`episode-checkbox-changed`**
- Отправляется при изменении любого чекбокса эпизода
- Содержит информацию: tvId, seasonNumber, episodeNumber, checked, isLastUnwatched, wasAllWatched
- Основные кнопки используют это для автоматических переходов состояний

---

## ✨ Что изменилось

1. **Упрощен код** - убраны сложные механизмы кеширования и батчинга
2. **Убраны избыточные проверки** - логика стала линейной и понятной
3. **Переписаны кнопки под сезонами** - простая и понятная логика
4. **Единый стиль** - все компоненты используют одинаковый подход
5. **Меньше событий** - только необходимые для работы
6. **Простая структура** - один метод для каждого действия

---

## 🔧 Технические детали

### Структура компонентов

```javascript
class MovieActionButtons {
    _movie          // данные фильма
    _state          // текущее состояние
    _render()       // создание HTML
    _updateUI()     // обновление интерфейса
    _handleXClick() // обработчики кликов
    _showXMenu()    // показ меню
}
```

### Принципы

1. Состояние хранится в `_state`
2. UI обновляется через `_updateUI()`
3. Все действия идут через сервис `userMoviesService`
4. События отправляются через `_dispatchEvent()`
5. Активность записывается через `_activityScreen`


---

## 📊 Краткая сводка: Кнопки под сезонами

### Три кнопки управления сезоном

1. **Mark as Watched** - отмечает все эпизоды сезона
2. **Mark as Unwatched** - снимает отметки со всех эпизодов сезона
3. **Rate Season** - открывает диалог оценки сезона

### Логика отображения

| Состояние сезона | Кнопки |
|------------------|--------|
| Не полностью просмотрен | Mark as Watched |
| Полностью просмотрен, нет оценки | Rate Season + Mark as Unwatched |
| Полностью просмотрен, есть оценка | Mark as Unwatched |

### Автоматические переходы

- **Mark as Watched** → если это последний непросмотренный сезон → сериал переходит в WATCHED
- **Mark as Unwatched** → если сериал был в WATCHED → сериал переходит в WATCHING
- **Чекбокс эпизода** → работает так же, как кнопки сезона, но для одного эпизода

### Ключевые особенности

- Кнопки работают только с текущим отображаемым сезоном
- Автоматически проверяют общее состояние всех сезонов
- Отправляют события для синхронизации с основными кнопками
- Обновляют UI мгновенно для отзывчивости
