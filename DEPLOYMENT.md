# 🚀 Деплой на GitHub Pages

## Быстрый старт

### 1. Подготовка репозитория

Если репозиторий еще не создан:

```bash
# Инициализируйте Git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit"

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ваш-username/имя-репозитория.git

# Отправьте код
git branch -M main
git push -u origin main
```

### 2. Настройка секретов GitHub

1. Перейдите в ваш репозиторий на GitHub
2. **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"** и добавьте каждый секрет:

   | Имя секрета | Значение |
   |------------|----------|
   | `VITE_TMDB_API_KEY` | Ваш ключ API от TMDB |
   | `VITE_BOT_TOKEN` | Токен вашего Telegram бота |
   | `VITE_BOT_USERNAME` | Имя вашего Telegram бота (без @) |

### 3. Включение GitHub Pages

1. **Settings** → **Pages**
2. В разделе **Source** выберите **"GitHub Actions"**
3. Сохраните настройки

### 4. Обновление конфигурации

В файле `vite.config.js` замените `'my-mini-app'` на **реальное имя вашего репозитория**:

```javascript
base: process.env.GITHUB_PAGES === 'true' ? '/имя-вашего-репозитория/' : '/',
```

Например, если ваш репозиторий называется `telegram-movie-app`:

```javascript
base: process.env.GITHUB_PAGES === 'true' ? '/telegram-movie-app/' : '/',
```

### 5. Деплой

После настройки просто пушьте изменения:

```bash
git add .
git commit -m "Configure GitHub Pages deployment"
git push
```

GitHub Actions автоматически:
- ✅ Установит зависимости
- ✅ Соберет проект
- ✅ Задеплоит на GitHub Pages

## Проверка деплоя

1. Перейдите в **Actions** в вашем репозитории
2. Дождитесь завершения workflow "Deploy to GitHub Pages"
3. После успешного деплоя, сайт будет доступен по адресу:
   ```
   https://ваш-username.github.io/имя-репозитория/
   ```

## Настройка Telegram Bot

После деплоя, настройте ваш Telegram бот:

1. Найдите @BotFather в Telegram
2. Отправьте команду:
   ```
   /setmenubutton
   ```
3. Выберите вашего бота
4. Укажите URL вашего приложения:
   ```
   https://ваш-username.github.io/имя-репозитория/
   ```

## Troubleshooting

### Проблема: 404 на GitHub Pages

**Решение:** Проверьте, что:
- В Settings → Pages выбран источник "GitHub Actions"
- Workflow успешно завершился (зеленая галочка в Actions)
- Base path в `vite.config.js` совпадает с именем репозитория

### Проблема: Assets не загружаются

**Решение:** Убедитесь, что base path правильно настроен в `vite.config.js`

### Проблема: API ключи не работают

**Решение:** Проверьте, что все секреты добавлены в Settings → Secrets and variables → Actions

## Локальная разработка

Для локальной разработки используйте:

```bash
npm run dev
```

Для сборки без base path (для тестирования):

```bash
npm run build:local
```

## Автоматический деплой

Каждый push в ветку `main` автоматически запускает деплой. Вы также можете запустить деплой вручную:

1. **Actions** → **Deploy to GitHub Pages**
2. **Run workflow** → выберите ветку `main`
3. **Run workflow**

## Переход с Vercel

Если вы переходите с Vercel:

1. ✅ Удалите `vercel.json` (больше не нужен)
2. ✅ Перенастройте webhook Telegram бота на новый URL
3. ✅ Убедитесь, что все environment variables перенесены в GitHub Secrets

---

🎉 **Готово!** Ваше приложение теперь работает на GitHub Pages!

