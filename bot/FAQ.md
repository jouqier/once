# FAQ - Часто задаваемые вопросы

## Общие вопросы

### Нужно ли мне писать код бота?

**Нет, если вам нужен только базовый функционал:**
- Direct Link работает без бота
- Простой шаринг работает без бота

**Да, если вы хотите:**
- Шаринг с превью (красивые карточки)
- Обработку команды /start с параметрами
- Кастомные сообщения и кнопки

### Где запускать бота?

**Для разработки:**
- Локально на вашем компьютере

**Для production:**
- VPS (DigitalOcean, Linode, Hetzner)
- Heroku (бесплатный план)
- Railway
- Render
- Vercel (serverless функции)

### Сколько стоит запуск бота?

**Бесплатно:**
- Heroku (с ограничениями)
- Railway (бесплатный план)
- Render (бесплатный план)

**Платно:**
- VPS от $5/месяц
- Heroku Hobby от $7/месяц

## Технические вопросы

### Бот не запускается

**Ошибка: "Invalid bot token"**

Решение:
1. Проверьте `BOT_TOKEN` в `.env`
2. Убедитесь, что токен скопирован полностью
3. Нет лишних пробелов или переносов строк

**Ошибка: "TMDB API error"**

Решение:
1. Проверьте `TMDB_API_KEY` в `.env`
2. Убедитесь, что ключ активен на TMDB
3. Проверьте интернет-соединение

### Inline mode не работает

**Проблема:** При вводе `@bot_name` ничего не происходит

Решение:
1. Включите Inline Mode в BotFather:
   - `/mybots` → выберите бота → Bot Settings → Inline Mode → Turn on
2. Перезапустите бота
3. Подождите 1-2 минуты (кеш Telegram)

**Проблема:** Показывается "No results"

Решение:
1. Проверьте формат запроса: `share_movie_550`
2. Убедитесь, что фильм существует в TMDB
3. Проверьте логи бота на ошибки

### Не показываются постеры

**Проблема:** Карточки без изображений

Решение:
1. Проверьте, что у фильма есть постер в TMDB
2. Убедитесь, что URL постера доступен
3. Проверьте размер изображения (не более 5MB)

### Кнопка не открывает Mini App

**Проблема:** При нажатии ничего не происходит

Решение:
1. Проверьте `WEB_APP_URL` в `.env` бота
2. Убедитесь, что Web App настроен в BotFather
3. Проверьте `APP_SHORT_NAME` - должен совпадать с BotFather
4. Убедитесь, что приложение доступно по URL

## Вопросы по деплою

### Как задеплоить на Heroku?

1. Создайте аккаунт на Heroku
2. Установите Heroku CLI
3. В папке `bot/`:
   ```bash
   heroku login
   heroku create your-bot-name
   heroku config:set BOT_TOKEN=your_token
   heroku config:set TMDB_API_KEY=your_key
   heroku config:set WEB_APP_URL=your_url
   heroku config:set BOT_USERNAME=your_username
   heroku config:set APP_SHORT_NAME=app
   git push heroku main
   ```

### Как задеплоить на VPS?

1. Подключитесь к VPS по SSH
2. Установите Node.js:
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```
3. Клонируйте репозиторий:
   ```bash
   git clone your-repo
   cd bot
   ```
4. Установите зависимости:
   ```bash
   npm install
   ```
5. Создайте `.env` файл
6. Установите PM2:
   ```bash
   sudo npm install -g pm2
   ```
7. Запустите бота:
   ```bash
   pm2 start index.js --name movie-bot
   pm2 startup
   pm2 save
   ```

### Как обновить бота на VPS?

```bash
cd bot
git pull
npm install
pm2 restart movie-bot
```

## Вопросы по функционалу

### Можно ли добавить поиск фильмов?

Да! Добавьте в `index.js`:

```javascript
bot.on('inline_query', async (ctx) => {
    const query = ctx.inlineQuery.query;
    
    if (!query.startsWith('share_') && query.length > 2) {
        // Поиск по TMDB
        const results = await searchMovies(query);
        // Формируем результаты
        await ctx.answerInlineQuery(results);
    }
});
```

### Можно ли добавить рекомендации?

Да! Используйте TMDB API:

```javascript
const recommendations = await axios.get(
    `https://api.themoviedb.org/3/movie/${movieId}/recommendations`,
    { params: { api_key: TMDB_API_KEY } }
);
```

### Можно ли добавить уведомления?

Да! Используйте `bot.telegram.sendMessage()`:

```javascript
// Отправка уведомления пользователю
await bot.telegram.sendMessage(
    userId,
    '🎬 Вышел новый фильм!',
    Markup.inlineKeyboard([
        [Markup.button.webApp('Смотреть', directLink)]
    ])
);
```

### Можно ли добавить базу данных?

Да! Используйте любую БД:

**MongoDB:**
```bash
npm install mongoose
```

**PostgreSQL:**
```bash
npm install pg
```

**SQLite:**
```bash
npm install sqlite3
```

## Вопросы по безопасности

### Безопасно ли хранить токены в .env?

**Локально:** Да, если `.env` в `.gitignore`

**На сервере:** Используйте переменные окружения:
```bash
export BOT_TOKEN=your_token
```

**В production:** Используйте секреты:
- Heroku Config Vars
- Railway Variables
- GitHub Secrets

### Нужно ли шифровать данные?

**Токены:** Не храните в коде, используйте переменные окружения

**Пользовательские данные:** Если храните в БД - да, шифруйте

**API ключи:** Не коммитьте в Git

## Вопросы по производительности

### Сколько запросов может обработать бот?

**Telegram Bot API:**
- 30 сообщений в секунду
- 20 запросов в секунду к API

**TMDB API:**
- 40 запросов в 10 секунд
- 4 запроса в секунду

### Как оптимизировать бота?

1. **Кеширование:**
   ```javascript
   const cache = new Map();
   
   async function getMediaData(type, id) {
       const key = `${type}_${id}`;
       if (cache.has(key)) {
           return cache.get(key);
       }
       const data = await fetchFromTMDB(type, id);
       cache.set(key, data);
       return data;
   }
   ```

2. **Rate limiting:**
   ```javascript
   const rateLimit = require('telegraf-ratelimit');
   
   bot.use(rateLimit({
       window: 1000,
       limit: 3
   }));
   ```

3. **Очередь запросов:**
   ```javascript
   const Queue = require('bull');
   const queue = new Queue('tmdb-requests');
   ```

## Вопросы по мониторингу

### Как смотреть логи?

**PM2:**
```bash
pm2 logs movie-bot
```

**Heroku:**
```bash
heroku logs --tail
```

**Railway:**
Смотрите в веб-интерфейсе

### Как узнать, что бот упал?

**PM2:**
```bash
pm2 status
```

**Мониторинг:**
- UptimeRobot (бесплатно)
- Pingdom
- StatusCake

### Как настроить алерты?

**PM2:**
```bash
pm2 install pm2-slack
pm2 set pm2-slack:slack_url https://hooks.slack.com/...
```

**Telegram:**
Отправляйте себе уведомления при ошибках:
```javascript
bot.catch(async (err) => {
    await bot.telegram.sendMessage(
        YOUR_ADMIN_ID,
        `❌ Ошибка: ${err.message}`
    );
});
```

## Нужна помощь?

- 📖 См. [README.md](./README.md)
- 💡 См. [EXAMPLES.md](./EXAMPLES.md)
- ⚡ См. [QUICK_START.md](./QUICK_START.md)
- 🐛 Откройте Issue на GitHub
