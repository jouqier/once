# Что делать после деплоя на Heroku

## Шаг 1: Проверьте, что бот запущен (2 минуты)

### Проверка логов

```bash
heroku logs --tail --app your-app-name
```

**Что должно быть в логах:**
```
✅ TMDB API подключен
✅ Бот запущен успешно!
📱 Bot username: @onceappbot
🌐 Web App URL: https://your-app.pages.dev
🔗 App short name: app
```

**Если видите ошибки:**
- Проверьте переменные окружения: `heroku config --app your-app-name`
- Убедитесь, что все переменные заполнены

### Проверка статуса

```bash
heroku ps --app your-app-name
```

Должно быть:
```
=== web (Free): npm start
web.1: up 2024/01/15 12:34:56 (~ 1m ago)
```

## Шаг 2: Включите Inline Mode (1 минута)

Если еще не включили:

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите `@onceappbot`
4. Выберите "Bot Settings"
5. Выберите "Inline Mode"
6. Выберите "Turn on"
7. Введите placeholder: "Поиск фильмов..."

## Шаг 3: Проверьте команду /start (1 минута)

1. Откройте Telegram
2. Найдите вашего бота `@onceappbot`
3. Отправьте `/start`

**Ожидаемый результат:**
```
👋 Привет! Я бот для отслеживания фильмов и сериалов.

🎬 Открой приложение, чтобы начать!

[Кнопка: 🎬 Открыть приложение]
```

**Если бот не отвечает:**
- Проверьте логи: `heroku logs --tail`
- Убедитесь, что dyno запущен: `heroku ps`
- Перезапустите: `heroku restart`

## Шаг 4: Проверьте /start с параметром (1 минута)

Откройте эту ссылку в Telegram:
```
https://t.me/onceappbot?start=movie_550
```

**Ожидаемый результат:**
- Бот отправит постер Fight Club
- Сообщение с названием, рейтингом и описанием
- Кнопка "▶️ Открыть в приложении"

**Если не работает:**
- Проверьте логи на ошибки TMDB API
- Убедитесь, что `TMDB_API_KEY` правильный

## Шаг 5: Проверьте Inline Mode (2 минуты)

1. Откройте любой чат в Telegram (можно "Избранное")
2. Наберите: `@onceappbot share_movie_550`
3. Подождите 1-2 секунды

**Ожидаемый результат:**
- Появится карточка с постером Fight Club
- Название, рейтинг, описание
- Можно отправить в чат

**Если не работает:**
- Убедитесь, что Inline Mode включен в BotFather
- Проверьте логи: `heroku logs --tail`
- Подождите 2-3 минуты (кеш Telegram)
- Попробуйте другой фильм: `@onceappbot share_movie_680` (Pulp Fiction)

## Шаг 6: Проверьте в Mini App (3 минуты)

1. Откройте ваш Mini App в Telegram
2. Откройте любой фильм (например, Fight Club)
3. Нажмите кнопку "Поделиться" (в правом верхнем углу постера)
4. Выберите "Поделиться с превью"

**Ожидаемый результат:**
- Telegram откроет диалог выбора чата
- Появится карточка с постером для отправки
- После отправки в чате будет красивая карточка с кнопкой

**Если не работает:**
- Проверьте, что приложение пересобрано: `npm run build`
- Убедитесь, что переменные в `.env` приложения совпадают с ботом
- Проверьте консоль браузера на ошибки

## Шаг 7: Проверьте Direct Link (2 минуты)

1. В Mini App откройте любой фильм
2. Нажмите "Поделиться" → "Скопировать ссылку"
3. Отправьте ссылку себе в "Избранное"
4. Откройте ссылку

**Ожидаемый результат:**
- Mini App открывается автоматически
- Показывается страница с деталями фильма

**Формат ссылки должен быть:**
```
https://t.me/onceappbot/app?startapp=movie_550
```

## Шаг 8: Настройте мониторинг (опционально)

### Heroku Metrics

Посмотрите метрики:
```bash
heroku open --app your-app-name
```

Перейдите в "Metrics" → смотрите:
- Response time
- Memory usage
- Dyno load

### UptimeRobot (бесплатно)

1. Зарегистрируйтесь на [UptimeRobot](https://uptimerobot.com)
2. Добавьте новый монитор:
   - Type: HTTP(s)
   - URL: `https://your-app-name.herokuapp.com/health` (если добавили health check)
   - Monitoring Interval: 5 минут
3. Настройте алерты на email/Telegram

### Логирование

Для постоянного мониторинга логов:
```bash
heroku logs --tail --app your-app-name > bot.log &
```

## Troubleshooting

### Бот не отвечает

**Проблема:** Бот молчит на команды

**Решение:**
```bash
# Проверьте статус
heroku ps --app your-app-name

# Если dyno спит (Free plan)
heroku ps:scale web=1 --app your-app-name

# Перезапустите
heroku restart --app your-app-name

# Проверьте логи
heroku logs --tail --app your-app-name
```

### Ошибка "Application error"

**Проблема:** При открытии URL бота показывается ошибка

**Решение:**
```bash
# Проверьте логи
heroku logs --tail --app your-app-name

# Проверьте переменные
heroku config --app your-app-name

# Убедитесь, что все переменные заполнены
heroku config:set BOT_TOKEN=your_token --app your-app-name
```

### Inline mode не работает

**Проблема:** При вводе `@bot` ничего не происходит

**Решение:**
1. Убедитесь, что Inline Mode включен в BotFather
2. Подождите 2-3 минуты (кеш Telegram)
3. Проверьте логи на ошибки
4. Попробуйте перезапустить бота

### Dyno засыпает (Free plan)

**Проблема:** Бот не отвечает после 30 минут неактивности

**Решение:**

**Вариант 1:** Апгрейд на Hobby ($7/мес)
```bash
heroku ps:type hobby --app your-app-name
```

**Вариант 2:** Используйте UptimeRobot для пинга каждые 5 минут

**Вариант 3:** Переезжайте на Railway/Render (бесплатные планы без сна)

## Полезные команды Heroku

### Просмотр переменных
```bash
heroku config --app your-app-name
```

### Установка переменной
```bash
heroku config:set VARIABLE=value --app your-app-name
```

### Перезапуск
```bash
heroku restart --app your-app-name
```

### Логи
```bash
heroku logs --tail --app your-app-name
```

### Открыть в браузере
```bash
heroku open --app your-app-name
```

### Масштабирование
```bash
heroku ps:scale web=1 --app your-app-name
```

## Следующие шаги

### 1. Добавьте health check (рекомендуется)

В `index.js` добавьте:
```javascript
const express = require('express');
const app = express();

app.get('/health', (req, res) => {
    res.send('OK');
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
    console.log(`Health check на порту ${PORT}`);
});
```

Установите express:
```bash
npm install express
git add .
git commit -m "Add health check"
git push heroku main
```

### 2. Настройте webhook (опционально)

Для лучшей производительности:
```javascript
const WEBHOOK_URL = process.env.WEBHOOK_URL;

if (WEBHOOK_URL) {
    bot.telegram.setWebhook(`${WEBHOOK_URL}/webhook`);
    app.use(bot.webhookCallback('/webhook'));
} else {
    bot.launch();
}
```

### 3. Добавьте базу данных (опционально)

Для хранения статистики:
```bash
heroku addons:create heroku-postgresql:mini --app your-app-name
```

### 4. Настройте CI/CD (опционально)

Автоматический деплой при push в GitHub:
1. Подключите GitHub репозиторий в Heroku Dashboard
2. Включите "Automatic deploys"
3. Выберите ветку (main)

## Готово! 🎉

Теперь у вас:
- ✅ Бот работает 24/7 на Heroku
- ✅ Inline mode для шаринга с превью
- ✅ Direct Link работает
- ✅ Команда /start обрабатывается

Пользователи могут:
1. Открывать Mini App через бота
2. Делиться фильмами с красивыми карточками
3. Открывать фильмы по прямым ссылкам

## Нужна помощь?

- 📖 См. [README.md](./README.md)
- 💡 См. [FAQ.md](./FAQ.md)
- 🐛 Проверьте логи: `heroku logs --tail`
