# Быстрый старт бота за 5 минут

## Шаг 1: Установка (1 минута)

```bash
cd bot
npm install
```

## Шаг 2: Настройка .env (2 минуты)

Создайте файл `.env`:

```bash
cp .env.example .env
```

Откройте `.env` и заполните:

```env
BOT_TOKEN=7760619955:AAGx92Yd0NrHVBLue9I27dZ4bdqJv6lyH1s
TMDB_API_KEY=8e2566462de35ecf36a54f4a04235ca8
WEB_APP_URL=https://your-app.pages.dev
BOT_USERNAME=onceappbot
APP_SHORT_NAME=app
```

**Где взять:**
- `BOT_TOKEN` - уже есть у вас
- `TMDB_API_KEY` - уже есть у вас (из основного .env)
- `WEB_APP_URL` - URL где развернуто приложение
- `BOT_USERNAME` - имя бота без @
- `APP_SHORT_NAME` - короткое имя из BotFather

## Шаг 3: Включите Inline Mode (1 минута)

1. Откройте [@BotFather](https://t.me/BotFather)
2. `/mybots` → выберите бота → Bot Settings → Inline Mode → Turn on
3. Введите placeholder: "Поиск фильмов..."

## Шаг 4: Запуск (1 минута)

```bash
npm start
```

Вы должны увидеть:
```
✅ TMDB API подключен
✅ Бот запущен успешно!
📱 Bot username: @onceappbot
🌐 Web App URL: https://your-app.pages.dev
🔗 App short name: app

💡 Для остановки нажмите Ctrl+C
```

## Шаг 5: Проверка

### Проверка команды /start

1. Откройте чат с ботом в Telegram
2. Отправьте `/start`
3. Должна появиться кнопка "Открыть приложение"

### Проверка inline mode

1. Откройте любой чат в Telegram
2. Наберите: `@onceappbot share_movie_550`
3. Должна появиться карточка с постером Fight Club
4. Отправьте её в чат
5. Нажмите кнопку "Открыть в приложении"
6. Mini App должен открыться с деталями фильма

## Готово! 🎉

Теперь в вашем Mini App:
1. Откройте любой фильм
2. Нажмите кнопку "Поделиться"
3. Выберите "Поделиться с превью"
4. Отправьте красивую карточку с постером!

## Что дальше?

### Для разработки

Используйте nodemon для автоперезагрузки:
```bash
npm run dev
```

### Для production

Деплой на VPS с PM2:
```bash
npm install -g pm2
pm2 start index.js --name movie-bot
pm2 startup
pm2 save
```

Или деплой на Heroku/Railway (см. bot/README.md)

## Troubleshooting

**Бот не запускается:**
- Проверьте `.env` файл
- Убедитесь, что все переменные заполнены
- Проверьте токен бота

**Inline mode не работает:**
- Убедитесь, что включили Inline Mode в BotFather
- Проверьте логи бота на ошибки
- Попробуйте перезапустить бота

**Нужна помощь?**
- См. полную документацию в `bot/README.md`
- Проверьте логи: `pm2 logs movie-bot` (если используете PM2)
