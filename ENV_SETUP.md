# 🔐 Настройка переменных окружения

## Быстрая настройка

### 1. Создайте файл `.env`

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

### 2. Заполните переменные

Откройте `.env` и замените placeholder-значения на реальные:

```env
# TMDB API Configuration
VITE_TMDB_API_KEY=ваш_реальный_ключ_tmdb

# Telegram Bot Configuration  
VITE_BOT_TOKEN=ваш_реальный_токен_бота
VITE_BOT_USERNAME=имя_вашего_бота
```

## Получение ключей

### TMDB API Key

1. Зарегистрируйтесь на https://www.themoviedb.org/
2. Перейдите в Settings → API
3. Создайте новый API ключ (версия 3)
4. Скопируйте "API Key (v3 auth)"

### Telegram Bot

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен
5. Имя бота - это username без @

## Для деплоя на GitHub Pages

### Настройка секретов GitHub

1. Перейдите в ваш репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. Нажмите "New repository secret" и добавьте:
   - `VITE_TMDB_API_KEY` - ваш ключ TMDB
   - `VITE_BOT_TOKEN` - токен Telegram бота
   - `VITE_BOT_USERNAME` - имя Telegram бота

### Включение GitHub Pages

1. Settings → Pages
2. Source → выберите "GitHub Actions"
3. После пуша в ветку `main`, сайт автоматически задеплоится

### Обновление base path

В файле `vite.config.js` замените `'my-mini-app'` на имя вашего репозитория:

```javascript
base: process.env.GITHUB_PAGES === 'true' ? '/имя-вашего-репозитория/' : '/',
```

Ваш сайт будет доступен по адресу:
`https://ваш-username.github.io/имя-репозитория/`

## Проверка

Запустите проект:

```bash
npm run dev
```

Если все настроено правильно, приложение должно:
- ✅ Загружать данные о фильмах
- ✅ Работать с Telegram WebApp API
- ✅ Не показывать ошибок в консоли

## Безопасность

⚠️ **Важно:**
- Файл `.env` уже добавлен в `.gitignore`
- Никогда не коммитьте `.env` в Git
- Используйте `.env.example` только как шаблон
- Храните реальные ключи в безопасности

## Что изменилось

После внесения улучшений:

### До:
```javascript
// src/config/api.js
API_KEY: '8e2566462de35ecf36a54f4a04235ca8'  // ❌ Хардкод в коде
```

### После:
```javascript
// src/config/api.js
API_KEY: import.meta.env.VITE_TMDB_API_KEY || 'fallback_key'  // ✅ Из переменных окружения
```

Теперь ваши ключи:
- 🔒 Не хранятся в коде
- 🔒 Не попадают в Git
- 🔒 Легко меняются без изменения кода
- ✅ Разные для dev/production окружений

