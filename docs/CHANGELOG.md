# Changelog

Все важные изменения в проекте документируются в этом файле.

## [1.2.0] - 2024-11-08

### 🎯 Основные изменения

#### Разделение хранилищ фильмов и сериалов

Проведен рефакторинг структуры хранения данных для четкого разделения фильмов и сериалов.

### ✨ Добавлено

- **Новая структура данных** для сериалов с отдельными списками `want`, `watching`, `watched`
- **Новые методы API** для работы с сериалами:
  - `getTVShowState(showId)` - получение состояния сериала
  - `addTVShowToWant(show)` - добавление в "Хочу посмотреть"
  - `addTVShowToWatching(show)` - добавление в "Смотрю"
  - `addTVShowToWatched(show)` - добавление в "Посмотрел"
  - `removeTVShowFromWant(showId)` - удаление из "Хочу посмотреть"
  - `removeTVShowFromWatching(showId)` - удаление из "Смотрю"
  - `removeTVShowFromWatched(showId)` - удаление из "Посмотрел"
  - `getTVShowWantList()` - получение списка "Хочу посмотреть"
  - `getTVShowWatchingList()` - получение списка "Смотрю"
  - `getTVShowWatchedList()` - получение списка "Посмотрел"
- **Автоматическая миграция** данных с версии 1.1 на 1.2
- **Валидация типов контента** - защита от добавления фильмов в `watching` и смешивания типов
- **Тесты миграции** для проверки корректности переноса данных
- **Документация**:
  - `docs/STORAGE_SEPARATION.md` - подробное описание изменений
  - `docs/API_CHANGES_v1.2.md` - краткая справка для разработчиков
  - `tests/test-migration.js` - автоматические тесты

### 🔄 Изменено

- **Структура хранилища**: фильмы и сериалы теперь хранятся отдельно
- **UserMoviesService**: методы для фильмов теперь работают только с фильмами
- **UserDataStore**: добавлены отдельные методы `getTVShows()`, `addTVShow()`, `removeTVShow()`
- **ProfilePage**: обновлена логика получения списков для отображения
- **ShowCardButtons**: обновлены вызовы методов для работы с сериалами

### ❌ Удалено

- `movies.watching` - фильмы больше не могут быть в состоянии "watching"
- Смешивание фильмов и сериалов в одних списках

### 🐛 Исправлено

- Концептуальная ошибка: фильмы больше не могут быть в состоянии "watching"
- Путаница с типами контента при фильтрации списков
- Риск случайного добавления фильма в неправильное состояние

### 🔒 Безопасность

- Добавлена валидация типов контента при добавлении в списки
- Защита от смешивания фильмов и сериалов
- Автоматическая проверка `media_type` при операциях

### 📝 Миграция

Миграция происходит автоматически при первом запуске версии 1.2:
1. Создаются новые списки для сериалов в `tvShows`
2. Сериалы переносятся из `movies.*` в `tvShows.*`
3. Удаляется `movies.watching`
4. Выполняется валидация и очистка данных

Для проверки миграции запустите:
```bash
node tests/test-migration.js
```

### 📚 Документация

- См. [STORAGE_SEPARATION.md](docs/STORAGE_SEPARATION.md) для подробного описания
- См. [API_CHANGES_v1.2.md](docs/API_CHANGES_v1.2.md) для краткой справки

---

## [1.1.0] - Предыдущая версия

### Добавлено
- Базовая структура хранения данных
- Поддержка фильмов и сериалов
- Система отзывов и рейтингов
- Отслеживание эпизодов сериалов

---

## Формат

Этот changelog следует принципам [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
и проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

### Типы изменений

- **Добавлено** - новые функции
- **Изменено** - изменения в существующей функциональности
- **Устарело** - функции, которые скоро будут удалены
- **Удалено** - удаленные функции
- **Исправлено** - исправления багов
- **Безопасность** - исправления уязвимостей
