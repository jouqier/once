# Настройка Ngrok для локальной разработки

Ngrok позволяет создать HTTPS туннель к вашему локальному Vite dev server, что необходимо для тестирования Telegram Mini App локально.

## Быстрая настройка

### Шаг 1: Установка ngrok

```bash
# Установите ngrok глобально
npm install -g ngrok

# Или через Homebrew (macOS)
brew install ngrok/ngrok/ngrok

# Или скачайте с официального сайта
# https://ngrok.com/download
```

### Шаг 2: Получение токена

1. Зарегистрируйтесь на [ngrok.com](https://dashboard.ngrok.com/signup) (бесплатно)
2. Перейдите в раздел [Your Authtoken](https://dashboard.ngrok.com/get-started/your-authtoken)
3. Скопируйте ваш токен

### Шаг 3: Настройка конфигурации

Откройте файл `ngrok.yml` в корне проекта и замените `YOUR_NGROK_AUTH_TOKEN` на ваш токен:

```yaml
authtoken: ваш_токен_здесь
```

### Шаг 4: Запуск

#### Вариант 1: Автоматический запуск (рекомендуется)

Используйте скрипт, который автоматически запустит ngrok и Vite:

```bash
npm run dev:ngrok
```

Скрипт:
- ✅ Проверит наличие ngrok
- ✅ Запустит ngrok туннель
- ✅ Получит публичный URL
- ✅ Запустит Vite dev server

После запуска вы увидите:
```
✅ Ngrok туннель создан: https://abc123.ngrok.io
⚡ Запуск Vite dev server...
```

#### Вариант 2: Ручной запуск

1. Запустите ngrok в отдельном терминале:

```bash
ngrok start --config ngrok.yml app
```

2. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

3. В другом терминале запустите Vite:

```bash
npm run dev
```

4. Используйте ngrok URL для доступа к приложению

## Обновление Web App URL в BotFather

После получения ngrok URL, обновите его в BotFather:

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/setmenubutton`
3. Выберите вашего бота
4. Введите новый Web App URL (ваш ngrok URL)
5. Готово!

## Важные замечания

### ⚠️ Бесплатный план ngrok

- URL меняется при каждом перезапуске
- Нужно обновлять URL в BotFather каждый раз
- Ограничение: 1 туннель одновременно

### 💡 Платный план ngrok

Если вы хотите статический домен:

1. Купите план на [ngrok.com](https://ngrok.com/pricing)
2. Добавьте в `ngrok.yml`:

```yaml
tunnels:
  app:
    addr: 3000
    proto: http
    domain: your-custom-domain.ngrok.io
```

### 🔄 Автоматическое обновление URL

Для автоматического обновления URL в BotFather можно использовать Telegram Bot API:

```javascript
const axios = require('axios');

async function updateWebAppUrl(botToken, url) {
    await axios.post(`https://api.telegram.org/bot${botToken}/setChatMenuButton`, {
        menu_button: {
            type: 'web_app',
            text: 'Открыть приложение',
            web_app: { url: url }
        }
    });
}
```

## Использование с Telegram Mini App

1. Запустите `npm run dev:ngrok`
2. Скопируйте ngrok URL
3. Обновите Web App URL в BotFather
4. Откройте бота в Telegram
5. Нажмите кнопку меню бота
6. Mini App откроется с вашего локального сервера!

## Troubleshooting

### Ошибка: "ngrok не найден"

**Решение:**
```bash
# Проверьте установку
ngrok version

# Если не установлен, установите:
npm install -g ngrok
```

### Ошибка: "authtoken is required"

**Решение:**
1. Проверьте, что токен указан в `ngrok.yml`
2. Убедитесь, что нет лишних пробелов
3. Получите новый токен на [dashboard.ngrok.com](https://dashboard.ngrok.com/get-started/your-authtoken)

### Ошибка: "address already in use"

**Решение:**
Порт 3000 уже занят. Либо:
1. Остановите другой процесс на порту 3000
2. Или измените порт в `vite.config.js` и в `ngrok.yml`

### URL не работает в Telegram

**Решение:**
1. Убедитесь, что используете HTTPS URL (не HTTP)
2. Проверьте, что Vite dev server запущен
3. Убедитесь, что URL обновлен в BotFather
4. Проверьте консоль браузера на ошибки

### CORS ошибки

Если возникают CORS ошибки, добавьте в `vite.config.js`:

```javascript
server: {
    port: 3000,
    host: '0.0.0.0', // Разрешает доступ извне
    cors: true
}
```

## Альтернативы ngrok

Если ngrok не подходит, можно использовать:

- **Cloudflare Tunnel** (бесплатно, статический домен)
- **localtunnel** (npm пакет, бесплатно)
- **serveo** (SSH туннель, бесплатно)
- **Telebit** (бесплатно, статический домен)

## Полезные ссылки

- [Ngrok документация](https://ngrok.com/docs)
- [Ngrok dashboard](https://dashboard.ngrok.com)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [Vite документация](https://vitejs.dev)

