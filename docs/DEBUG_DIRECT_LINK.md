# Отладка Direct Link

## Проблема: Ссылка открывает чат вместо Mini App

Если ссылка `https://t.me/onceappbot/app?startapp=movie_550` открывает чат с ботом вместо Mini App, проверьте следующее:

## Шаг 1: Проверьте настройку Web App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/myapps`
3. Выберите вашего бота `@onceappbot`
4. Проверьте:
   - ✅ **Web App URL** должен быть указан (ваш реальный URL приложения)
   - ✅ **Short name** должен быть `app` (или то, что указано в `VITE_APP_SHORT_NAME`)

### Если Web App не создан:

1. Отправьте `/newapp` в BotFather
2. Выберите `@onceappbot`
3. Заполните все поля:
   - Title: `Movie Tracker`
   - Description: `Track movies and TV shows`
   - Photo: загрузите квадратное изображение
   - GIF: отправьте `/empty`
   - **Web App URL**: ваш реальный URL (например, `https://your-app.pages.dev`)
   - **Short name**: `app`

### Если Web App уже создан, но короткое имя другое:

Вариант 1: Измените `VITE_APP_SHORT_NAME` в `.env` на то, что указано в BotFather

Вариант 2: Создайте новый Web App с коротким именем `app`

## Шаг 2: Проверьте формат ссылки

Откройте консоль браузера (F12) в вашем приложении и выполните:

```javascript
import { shareLinkService } from './src/services/share-link.js';
const link = shareLinkService.generateTelegramLink(550, 'movie');
console.log(link);
```

**Правильный формат:**
```
https://t.me/onceappbot/app?startapp=movie_550
```

**Неправильные форматы:**
```
https://t.me/onceappbot?start=movie_550  ❌ (это старый формат, открывает чат)
https://t.me/onceappbot/app              ❌ (нет параметра startapp)
https://your-app.com/?id=550&type=movie  ❌ (это веб-ссылка)
```

## Шаг 3: Проверьте переменные окружения

В консоли браузера:

```javascript
console.log('BOT_USERNAME:', import.meta.env.VITE_BOT_USERNAME);
console.log('APP_SHORT_NAME:', import.meta.env.VITE_APP_SHORT_NAME);
```

Должно вывести:
```
BOT_USERNAME: onceappbot
APP_SHORT_NAME: app
```

Если выводит `undefined`, значит:
1. Не пересобрали приложение после изменения `.env`
2. Переменные названы неправильно (должны начинаться с `VITE_`)

**Решение:**
```bash
npm run build
```

## Шаг 4: Проверьте URL в BotFather

Web App URL в BotFather должен совпадать с URL, где развернуто приложение.

**Проверка:**
1. Откройте URL из BotFather в браузере
2. Приложение должно загрузиться
3. Если не загружается - проблема в деплое, а не в Direct Link

## Шаг 5: Тест Direct Link вручную

Попробуйте открыть ссылку напрямую в Telegram:

1. Скопируйте: `https://t.me/onceappbot/app?startapp=movie_550`
2. Отправьте себе в "Избранное" в Telegram
3. Нажмите на ссылку

**Что должно произойти:**
- ✅ Telegram открывает Mini App автоматически
- ✅ Приложение загружается с деталями фильма Fight Club (ID 550)

**Что НЕ должно произойти:**
- ❌ Открывается чат с ботом
- ❌ Бот отправляет сообщение
- ❌ Нужно нажимать кнопку для открытия приложения

## Шаг 6: Проверьте обработку параметра в приложении

Откройте консоль в Mini App после перехода по ссылке:

```javascript
console.log('URL params:', new URLSearchParams(window.location.search).get('startapp'));
console.log('TG start_param:', window.Telegram?.WebApp?.initDataUnsafe?.start_param);
```

Должно вывести:
```
URL params: movie_550
TG start_param: movie_550
```

Если выводит `null` - параметр не передается, проблема в формате ссылки.

## Частые причины проблем

### 1. Web App не создан в BotFather
**Симптом:** Ссылка открывает чат с ботом

**Решение:** Создайте Web App через `/newapp` в BotFather

### 2. Неправильное короткое имя
**Симптом:** Ссылка не работает или открывает чат

**Решение:** Убедитесь, что `VITE_APP_SHORT_NAME` совпадает с коротким именем в BotFather

### 3. Неправильный URL в BotFather
**Симптом:** Mini App открывается, но показывает ошибку

**Решение:** Проверьте, что URL в BotFather совпадает с реальным URL приложения

### 4. Не пересобрали после изменения .env
**Симптом:** Генерируются старые ссылки

**Решение:** 
```bash
npm run build
```

### 5. У бота несколько Web Apps
**Симптом:** Открывается не то приложение

**Решение:** Проверьте все Web Apps бота через `/myapps` в BotFather

## Альтернативное решение

Если Direct Link не работает, можно использовать Menu Button:

1. В BotFather отправьте `/mybots`
2. Выберите `@onceappbot`
3. Выберите "Bot Settings" → "Menu Button"
4. Выберите "Configure menu button"
5. Отправьте текст кнопки: `🎬 Open App`
6. Отправьте URL: ваш Web App URL

Теперь в чате с ботом будет кнопка меню для открытия приложения.

**Но:** Menu Button не поддерживает параметры, поэтому прямые ссылки на конкретные фильмы работать не будут.

## Проверочный чек-лист

- [ ] Web App создан в BotFather (`/myapps`)
- [ ] Короткое имя в BotFather = `app`
- [ ] `VITE_BOT_USERNAME=onceappbot` в `.env`
- [ ] `VITE_APP_SHORT_NAME=app` в `.env`
- [ ] Приложение пересобрано (`npm run build`)
- [ ] Ссылка имеет формат `t.me/onceappbot/app?startapp=...`
- [ ] URL в BotFather совпадает с реальным URL приложения
- [ ] Приложение доступно по URL из BotFather

Если все пункты выполнены, но не работает - напишите в поддержку Telegram, возможно проблема на их стороне.
