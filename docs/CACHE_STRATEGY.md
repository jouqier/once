# Стратегия кеширования и хранения данных

## Принципы

### 🔴 Критичные данные (НИКОГДА не теряем)

Хранятся в `localStorage` с ключом `user_data_${userId}`:

- **Списки фильмов**: want, watched
- **Списки сериалов**: want, watching, watched  
- **Прогресс серий**: какие эпизоды просмотрены
- **Рейтинги**: оценки фильмов и сезонов
- **Активность**: история действий пользователя
- **Недавний поиск**: последние поисковые запросы

**Реализация**: `src/services/user-data-store.js`

**Гарантии**:
- Сохранение при каждом изменении
- Миграция данных при обновлении версии
- Валидация структуры данных
- Разделение по пользователям

### 🟡 Кешируемые данные (можно потерять и перезапросить)

#### 1. Метаданные из TMDB API

**Что**: детали фильмов/сериалов (название, постер, описание и т.д.)

**TTL**: 24 часа

**Хранение**: localStorage с префиксом `cache_`

**Причина**: метаданные редко меняются, экономим API запросы

```javascript
// Пример
cache_movie_123 = {
  value: { id: 123, title: "...", poster_path: "..." },
  timestamp: 1699123456789
}
```

#### 2. Списки из TMDB

**Что**: trending, popular, upcoming, top rated

**TTL**: 
- Trending: 30 минут
- Popular/Upcoming: 1 час

**Хранение**: localStorage с префиксом `cache_`

**Причина**: списки обновляются редко, ускоряем загрузку главной

```javascript
cache_movies_trending = {
  value: [...],
  timestamp: 1699123456789
}
```

#### 3. Персонализированные рекомендации

**Что**: рекомендации на основе списков пользователя

**TTL**: 5 минут

**Хранение**: memory cache (Map) - НЕ сохраняется между сессиями

**Инвалидация**: при изменении списков want/watched/watching

**Причина**: 
- Рекомендации зависят от списков пользователя
- При изменении списков нужны новые рекомендации
- Короткий TTL для актуальности

```javascript
// В памяти
memoryCache.set('movies_recommended_123,456,789', {
  value: [...],
  timestamp: 1699123456789
})
```

#### 4. Вычисленный прогресс сериалов

**Что**: результат расчета "15 из 20 эпизодов"

**TTL**: до инвалидации по событию

**Хранение**: memory cache (Map)

**Инвалидация**: при изменении статуса эпизода

**Причина**: 
- Исходные данные (какие эпизоды просмотрены) в user_data
- Вычисление прогресса требует запроса к TMDB (общее число эпизодов)
- Кешируем результат вычисления, не исходные данные

```javascript
// В памяти
progressCache.set(123, {
  showId: 123,
  watchedEpisodes: 15,
  totalEpisodes: 20
})
```

## Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    localStorage                          │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  user_data_123456 (КРИТИЧНО)                            │
│  ├── movies: { want: [], watched: [], reviews: {} }     │
│  ├── tvShows: { want: [], watching: [], watched: [],    │
│  │              episodes: {}, reviews: {} }              │
│  ├── activity: []                                        │
│  └── search: { recent: [] }                              │
│                                                           │
│  cache_movies_trending (можно потерять)                 │
│  cache_movies_popular (можно потерять)                  │
│  cache_movie_123 (можно потерять)                       │
│  cache_tv_456 (можно потерять)                          │
│                                                           │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                    Memory Cache (Map)                    │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  movies_recommended_123,456 (теряется при перезагрузке) │
│  tv_recommended_789,012 (теряется при перезагрузке)     │
│  progress_123 (теряется при перезагрузке)               │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

## API

### CacheStrategy

```javascript
import { cacheStrategy, TTL, CACHE_KEYS } from './cache-strategy.js';

// Получить из кеша
const data = cacheStrategy.get('key', {
  persistent: true,  // использовать localStorage
  ttl: TTL.TRENDING_LISTS  // время жизни
});

// Сохранить в кеш
cacheStrategy.set('key', data, {
  persistent: true  // сохранить в localStorage
});

// Удалить из кеша
cacheStrategy.delete('key', { persistent: true });

// Инвалидировать по паттерну
cacheStrategy.invalidatePattern('recommended');

// Очистить весь кеш (НЕ трогает user_data!)
cacheStrategy.clearAll({ persistent: true });
```

### TMDBCacheService

```javascript
import TMDBCacheService from './tmdb-cache.js';

// Автоматически использует кеш
const trending = await TMDBCacheService.getTrendingMovies();

// Инвалидировать рекомендации при изменении списков
TMDBCacheService.invalidateRecommendations();
```

## Миграция

### Шаг 1: Заменить импорты

```javascript
// Было
import TMDBService from './services/tmdb.js';

// Стало
import TMDBCacheService from './services/tmdb-cache.js';
```

### Шаг 2: Удалить старый кеш

```javascript
// Удалить старые реализации
MoviesScreen._cache = null;
localStorage.removeItem('tvShowsData');
```

### Шаг 3: Использовать новый API

```javascript
// Автоматически кешируется
const trending = await TMDBCacheService.getTrendingMovies();

// Инвалидация при изменении списков
document.addEventListener('movie-list-changed', () => {
  TMDBCacheService.invalidateRecommendations();
});
```

## Преимущества

1. **Безопасность**: критичные данные защищены, кеш можно безопасно очистить
2. **Единообразие**: один подход для фильмов и сериалов
3. **Производительность**: умное кеширование с TTL
4. **Гибкость**: легко настроить TTL для разных типов данных
5. **Отладка**: легко очистить кеш без потери пользовательских данных

## Отладка

```javascript
// В консоли браузера

// Очистить весь кеш (НЕ трогает user_data)
cacheStrategy.clearAll({ persistent: true });

// Посмотреть размер кеша
cacheStrategy.getSize();
// { memory: 5, persistent: 10 }

// Инвалидировать рекомендации
TMDBCacheService.invalidateRecommendations();
```

## Мониторинг

Размер localStorage можно проверить:

```javascript
// Общий размер
let total = 0;
for (let key in localStorage) {
  if (localStorage.hasOwnProperty(key)) {
    total += localStorage[key].length + key.length;
  }
}
console.log(`localStorage size: ${(total / 1024).toFixed(2)} KB`);

// Размер user_data
const userData = localStorage.getItem('user_data_123456');
console.log(`user_data size: ${(userData.length / 1024).toFixed(2)} KB`);

// Размер кеша
let cacheSize = 0;
for (let key in localStorage) {
  if (key.startsWith('cache_')) {
    cacheSize += localStorage[key].length + key.length;
  }
}
console.log(`cache size: ${(cacheSize / 1024).toFixed(2)} KB`);
```
